{# work/_entry.twig #}

{% extends "_layouts/_layout.twig" %}
{% import '_macros/picture.twig' as picture %}

{% block content %}
<div class="container mx-auto">
  {# <h1 class="text-4xl font-bold mb-8">{{ entry.title }}</h1> #}

  <div class="container-image-prose">
    {% if entry.photo.one() %}
      {{ picture.portfolio(entry.photo.one(), entry.title, 600, 'w-full h-auto max-w-[800px] mx-auto object-cover card-art-work__image portfolio-img') }}
    {% endif %}

  </div>


  <div class="just the prose">
     {# {% if entry.essay %}
      <div class="prose max-w-4xl mx-auto">
        {{ entry.essay|nl2br }}

        <h2 class="text-2xl font-bold">
          {{ entry.title }}
        </h2>
      </div>
    {% endif %} #}


    <p style="color: lightgrey;margin-top: 30px; display:none;">
    A stroke, organ failure, near death,
      Thoughts of love, immense gratitude, creeping fear of never seeing my wife, daughter, family and friends again. The stroke had taken words from my mind, leaving only intense emotions and ideas. Phantoms. Pure love. A fear of never seeing anyone again.
    </p>
  </div>

  <div class="mt-8">
    <a href="/work" class="text-blue-600 hover:underline ">← Back to Work</a>
  </div>
</div>
{% endblock %}
